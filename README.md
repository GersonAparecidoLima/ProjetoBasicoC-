# Projeto de Acesso a Banco de Dados com ADO.NET e Padrão DAL

## 🗂️ Descrição

Este projeto demonstra como criar e manipular um banco de dados **SQL Server** utilizando o **Visual Studio** e a tecnologia **ADO.NET**.  
O objetivo é implementar uma camada de acesso a dados (DAL - *Data Access Layer*) que encapsule as operações com o banco de forma organizada e reutilizável.

---

## ⚙️ Funcionalidades

- Criação de um banco de dados e tabela no **SQL Server** utilizando o **Pesquisador de Objetos** do Visual Studio.  
- Conexão da aplicação ao banco de dados via **ADO.NET**.  
- Implementação do padrão **DAL (Data Access Layer)** para abstração do acesso aos dados.  
- Criação, leitura, atualização e exclusão de registros (**CRUD**) diretamente pela aplicação.  
- Testes dos métodos de acesso aos dados dentro da própria aplicação.

